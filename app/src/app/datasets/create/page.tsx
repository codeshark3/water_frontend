import React from "react";
import CreateDatasetForm from "~/components/forms/CreateDatasetForm";

const page = async () => {
  return (
    <div>
      <CreateDatasetForm />
    </div>
  );
};

export default page;
